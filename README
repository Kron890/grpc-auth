В разработке: gRPC-сервис аутентификации и авторизации.
Реализует регистрацию пользователей, вход, JWT-токенизацию (Access/Refresh) и верификацию прав доступа через gRPC.
Используется Redis для управления сессиями и безопасного хранения учетных данных.

go run cmd/main.go --config=./config/local.yaml

protoc --go_out=. --go-grpc_out=. ./contract/proto/auth.proto



TODO: Сделать ошибки по sql запросам
настроить более удоюнол логгер, чтобы он показывал,в каком методе возникла ошибка,
добавить Комменты к методам 
также по возможности dockerfile после завершения проекта 

upd: изменен config, добавлен redis 
изменен local.yaml добавлен redis 
изменен название postrges на storage и добавлен redis.go 
изменен директория repository: добавлена  postrges/postgres.go redisRepo/redis.go
добавлен  shutdown для postgres